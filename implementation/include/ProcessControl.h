#ifndef PROCESSCONTROL_H
#define PROCESSCONTROL_H

namespace ProcessControl {
    bool killProcess(int pid) {
        return true;
    }

    bool changeProcessPriority(int pid, int priority) {
        return true;
    }
}

#endif // PROCESSCONTROL_H
